# Ansible Docker Deployment

This project demonstrates how to automate Docker installation and container deployment using Ansible.

Features:
- Install Docker
- Start Docker service
- Deploy Nginx container
- Expose container on port 8080

Run:

ansible-playbook -i inventory docker_deploy.yml
